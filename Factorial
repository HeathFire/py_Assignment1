x=int(input("Enter a Whole Number "))
def fact(x):
    res=1
    for i in range(1, x+1):
        if (i>=1):
            res=i*fact(x-1)
        if (i<0):
            print("INVALID INPUT")
    return res
print(fact(x))
